import React from "react";

export const metadata = {
  title: "Kick | Settings",
  description: "Kick trivia dashboard",
};

const page = () => {
  return (
    <div className="p-24 flex justify-center items-center text-white">
      Settings page
    </div>
  );
};

export default page;
